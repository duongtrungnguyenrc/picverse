"use client";

import { FC } from "react";

type FloatingConversationsProps = {};

const FloatingConversations: FC<FloatingConversationsProps> = ({}) => {
  return <div className="fixed z-50 bottom-5 right-5 flex-center space-x-2">
    
  </div>
};

export default FloatingConversations;
