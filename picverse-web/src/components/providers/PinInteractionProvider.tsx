"use client"

import { FC, ReactNode } from 'react';

type PinInteractionProviderProps = {
    children: ReactNode
}

const PinInteractionProvider: FC<PinInteractionProviderProps> = ({children}) => {
    return <></>
}

export default PinInteractionProvider;