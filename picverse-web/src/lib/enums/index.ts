export enum EResourceType {
  FOLDER = "folder",
  FILE = "file",
}

export enum ECloudStorage {
  LOCAL = "local",
  DRIVE = "drive",
  DROPBOX = "dropbox",
}

export enum EGender {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}
