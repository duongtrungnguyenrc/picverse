import { FC, ReactNode } from "react";

type SignInCallbackLoadingProps = {
  children: ReactNode;
};

const SignInCallbackLoading: FC<SignInCallbackLoadingProps> = ({ children }) => {
  return children;
};

export default SignInCallbackLoading;
