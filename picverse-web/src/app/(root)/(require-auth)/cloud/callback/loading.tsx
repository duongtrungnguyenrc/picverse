import { FC, ReactNode } from "react";

type CloudCallbackLoadingProps = {
  children: ReactNode;
};

const CloudCallbackLoading: FC<CloudCallbackLoadingProps> = ({ children }) => {
  return children;
};

export default CloudCallbackLoading;
